pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
         echo "Testing Tools installation" 
         sh node --version
         sh git --version
         sh curl --version
      }
    }
  }
}
