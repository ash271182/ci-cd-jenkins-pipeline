pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
         echo "Testing Tools installation" 
          node --version
          git --version
          curl --version'''
      }
    }
  }
}
